// Full-page vault view — Phase 3 placeholder.
// Will reuse the Terminal frontend's Lit components (three-column layout,
// entry editor, folder tree, etc.) with the extension's HMAC network layer.

// For now, redirect to the popup by opening the extension popup.
console.log('PlusEV Secrets full-page vault — coming in Phase 3.');
